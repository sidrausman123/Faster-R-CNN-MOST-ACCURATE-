# Faster-R-CNN-MOST-ACCURATE-
This project demonstrates object detection on a CIFAR-10 image using a pre-trained Faster R-CNN with ResNet50-FPN in PyTorch. The model performs inference on a resized CIFAR image, predicts bounding boxes with confidence scores, and visualizes detected objects using OpenCV in Google Colab.
